<h1>hi</h1>
<div id="content" ng-controller="PostsCtrl">
  <form ng-submit="addEntry()">
    <input type="text" ng-model="newEntry.title">
    <input type="submit" value="Add">
  </form>
  <div ng-view></div>
  <ul>
    <li ng-repeat="post in posts">
      {{post.title}}
    </li>
  </ul>
</div>
{{newEntry.name}}